# ใช้ relative path เพื่อให้ทำงานได้ทั้ง localhost และเมื่อแชร์ผ่าน Cloudflare Tunnel
# Next.js จะ rewrite /api/* → backend ภายในเครื่อง (ดู next.config.ts)
NEXT_PUBLIC_API_BASE_URL=/api
NEXT_PUBLIC_AUTH_LOGIN_ENDPOINT=/auth/login
